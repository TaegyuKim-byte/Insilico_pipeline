# Base URL of the FastAPI backend (see docs/architecture.md).
# Leave empty to call relative "/api/..." paths (works behind a reverse
# proxy or a Next.js rewrite). Once the backend branch is merged in, point
# this at its dev server, e.g. http://localhost:8000
NEXT_PUBLIC_API_BASE_URL=
